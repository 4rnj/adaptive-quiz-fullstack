/**
 * Session Wizard Page - Multi-source session creation wizard
 */

import React from 'react';
import { SessionWizard } from '@/components/session/SessionWizard';

export const SessionWizardPage: React.FC = () => {
  return <SessionWizard />;
};